on:
  push:
    - main
    - release/*
  jobs:
    generate_docs:
      name: Deploy ontology documentation
      runs-on: ubuntu-18.04
      steps:
        name: Run build script
        run: ./run.sh
        shell: bash
